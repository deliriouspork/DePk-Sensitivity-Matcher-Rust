[package]
name = "DePk-Sensitivity-Matcher-Rust"
version = "1.0.0"
edition = "2024"

[dependencies]
evdev = "0.12"
slint = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"

[build-dependencies]
slint-build = "1"
